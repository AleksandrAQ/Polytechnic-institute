#!/bin/bash
#перенос всех файлов с расширением .png из каталога $pat1, в каталог pat2
pat1=$HOME/Изображения
pat2=$(pwd)
echo -e "путь1 = $pat1;\nпуть2 = $pat2;"
find $pat1 -type f -name '*.png' -exec mv '{}' $pat2  \;