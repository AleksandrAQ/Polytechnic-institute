#!/bin/bash
#удаление файлов из каталога $pat, к которым не было обращений $day дней, с выводом запроса удаления
day=99	
pat=$(pwd)
echo -e "Директория удаления = $pat;\n"
find $pwd -type f -mtime +$day -print -exec rm -f '{}' \;

